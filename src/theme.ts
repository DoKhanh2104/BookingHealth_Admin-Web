import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#1A71B4',
    },
    secondary: {
      main: '#35A6CD',
    },
  },
  typography: {
    fontFamily: 'Poppins, sans-serif',
  },
});
